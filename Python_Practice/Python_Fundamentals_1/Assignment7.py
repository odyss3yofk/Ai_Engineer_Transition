# Ask for a temperature celcius input as a string, convert it to float, then calculate and print the temperature in farenhite.

temp_celcius = float(input("Enter temperature"))

temp_far = (temp_celcius*(9/5)) + 32

print(temp_far)
